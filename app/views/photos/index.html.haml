%h1 Listing photos

%table
  %tr
    %th Image ref
    %th Description
    %th Location
    %th
    %th
    %th

  - @photos.each do |photo|
    %tr
      %td= image_tag photo.image
      %td= photo.description
      %td= photo.location
      / Links commented out as breaking app
      / %td= link_to 'Show', photo
      / %td= link_to 'Edit', edit_photo_path(photo)
      / %td= link_to 'Destroy', photo, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

/ Links commented out as breaking app
/ = link_to 'New Photo', new_photo_path
