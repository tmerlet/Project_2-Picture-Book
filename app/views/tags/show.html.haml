%p#notice= notice

%p
  %b Name:
  = @tag.name

%p
  %b Photo(s): (does work just no images to display)
  - @tag.photos.each do |photo_tag|
    = link_to photo_tag.location, album_photo_path(@album, photo_tag)


= link_to 'Edit', edit_tag_path(@tag)
\|
= link_to 'Back', tags_path
